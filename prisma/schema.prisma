// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum AccountStatus {
  NotLogged
  Logged
  NotExist
  WrongPassword
  TwoFactorAuthFailed
  Blocked
  VerificationRequired
}

model Account {
  id                  String @id @default(uuid())
  username            String @unique
  email               String
  password            String
  twoFactorAuthSecret String
  status              AccountStatus @default(NotLogged)
  sessionData         Json?

  proxyId             String?    @unique
  proxy               Proxy?     @relation(fields: [proxyId], references: [id], onDelete: Cascade)
}

model Proxy {
  id        String @id @default(uuid())
  proxyUrl  String @unique
  proxyPort Int @unique

  account   Account?
}

model Campaign {
  id   String @id @default(uuid())
  name String @unique
  data Json // { unsername: string, id: string }[]
}